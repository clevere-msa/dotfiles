# /commit

Use the committer helper to create a commit (exact paths only).

Ask for:
- Commit message
- Exact file paths to stage

Then run:
`~/agent-scripts/scripts/committer "<message>" <path> [<path> ...]`
