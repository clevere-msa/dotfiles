#!/bin/sh

LC_USERNAME=$(echo ${LOGNAME} | tr "[:upper:]" "[:lower:]")

REPODIR="/tmp/$LC_USERNAME"
ORIGDIR=$(pwd)

if  [ -d $REPODIR ]
then
    rm -rf $REPODIR
fi

mkdir $REPODIR
chmod 700 REPODIR 
cd $REPODIR

git clone ssh://git@gitlab.us.bank-dns.com:2222/clevere/my_secrets.git .
