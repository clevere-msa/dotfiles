# Tests

- Not run (wrapper config changes only).
- Not run (removed obsolete scripts only).
- Not run (removed aic alias only).
- Not run (shell startup guard changes only).
- Not run (metadata + port range change only).
- Not run (env sourcing/secret loader changes only).
- Not run (added with-secrets wrapper only).
- Not run (added with-secrets --env option only).
- Not run (added load-pass-from-tsv script only).
- Not run (load-pass-from-tsv verbosity change only).
- Not run (wrapper default add-dir + doc path restriction only).
- Not run (wrapper default add-dir file loading only).
- Not run (added add_dirs.txt template only).
- Not run (changed files tracking + codex-copy-changes helper only).
- Not run (codex-deploy-sync helper + deploy_repo_map.txt template only).
- Not run (codex-repo-sync helper only).
